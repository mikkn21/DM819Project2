
import fortunes
from point import Point

def test_always_true():
    assert True

# def test_parabel_intersection():
#     breakpoint = fortunes.find_breakpoint(Point(2, 3), Point(5, 3), -1)
#     assert breakpoint.is_close_to(Point(3.5, 1.28)) # Approx result
